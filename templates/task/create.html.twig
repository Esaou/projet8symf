{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('task_create')}) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success pull-right"><i class="fa-solid fa-plus left"></i> Ajouter</button>
            <a href="{{ path('task_list') }}" class="btn btn-primary">Liste des tâches</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
